---
name: googletasks
description: Google Tasks provides a simple to-do list and task management system integrated into Gmail and Google Calendar for quick and easy tracking integration via Composio MCP. Use when user needs to interact with Google Tasks.
---

# Google Tasks Integration

> Auto-generated by Composio Skills Extension on 2026-07-11T01:15:15.467Z
> **Execution**: Via Composio MCP Server (native Claude Code integration)

## Overview

Google Tasks provides a simple to-do list and task management system integrated into Gmail and Google Calendar for quick and easy tracking

This skill uses **Composio MCP** for tool execution. Tools are available as native MCP tools
that Claude can call directly without any Bash commands or scripts.

## MCP Configuration

This integration requires Composio MCP server to be configured in `.mcp.json`:

```json
{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": ["-y", "@composio/mcp"],
      "env": {
        "COMPOSIO_API_KEY": "your_api_key",
        "COMPOSIO_ENTITY_ID": "your_entity_id"
      }
    }
  }
}
```

## Available MCP Tools

### GOOGLETASKS_BATCH_EXECUTE

**MCP Tool**: `mcp__composio__GOOGLETASKS_BATCH_EXECUTE`

Executes multiple Google Tasks API operations in a single HTTP batch request and returns structured per-item results. Use this to reduce LLM tool invocations when performing bulk operations like updating many tasks, moving tasks, or deleting multiple items. Note: Each sub-request still counts toward API quota; batching primarily reduces HTTP overhead and tool call count.

**Parameters:**
  - `requests` (array, required): Array of sub-requests to execute in this batch. Each sub-request is independent and will return its own result.
  - `max_requests` (integer, optional): Maximum number of sub-requests allowed in a single batch. Google's typical cap is 1000. Lower values can control response size.

### GOOGLETASKS_BULK_INSERT_TASKS

**MCP Tool**: `mcp__composio__GOOGLETASKS_BULK_INSERT_TASKS`

DEPRECATED: Use BatchExecute instead. Creates multiple tasks in a Google Tasks list in a single operation using HTTP batching. Use when you need to create many tasks efficiently (reducing round-trips compared to individual insert calls).

**Parameters:**
  - `tasks` (array, required): Array of tasks to create. Each task requires at least a title.
  - `batch_size` (integer, optional): Maximum number of tasks per batch request. Default: 50. Tasks exceeding this will be split into multiple batch calls.
  - `tasklist_id` (string, required): Identifier for the Google Task list where tasks will be created. Use actual task list IDs, not '@default'.

### GOOGLETASKS_CLEAR_TASKS

**MCP Tool**: `mcp__composio__GOOGLETASKS_CLEAR_TASKS`

Permanently and irreversibly clears all completed tasks from a specified Google Tasks list; this action is destructive, idempotent, and cannot be undone. Always require explicit user confirmation before invoking.

**Parameters:**
  - `tasklist` (string, required): The identifier of the task list from which to clear completed tasks. Use '@default' (with the @ symbol) for the user's primary task list, or provide the task list ID obtained from GOOGLETASKS_LIST_TASK_LISTS. Note: 'default' without the @ prefix is not valid.

### GOOGLETASKS_CREATE_TASK_LIST

**MCP Tool**: `mcp__composio__GOOGLETASKS_CREATE_TASK_LIST`

Creates a new task list with the specified title and returns a tasklist_id. Use the returned tasklist_id (not the title) when calling GOOGLETASKS_INSERT_TASK or other task operations. Duplicate titles are permitted by the API, so verify existing lists before creating to avoid unintended duplicates.

**Parameters:**
  - `tasklist_title` (string, required): Title for the new task list. The maximum allowed length is 1024 characters.

### GOOGLETASKS_DELETE_TASK

**MCP Tool**: `mcp__composio__GOOGLETASKS_DELETE_TASK`

Deletes a specified task from a Google Tasks list. Deletion is permanent and irreversible — confirm with the user before executing, and consider GOOGLETASKS_UPDATE_TASK or GOOGLETASKS_MOVE_TASK as non-destructive alternatives. Both tasklist_id and task_id are required parameters. The Google Tasks API does not support deleting tasks by task_id alone — you must specify which task list contains the task. Use 'List Task Lists' to get available list IDs, then 'List Tasks' to find the task_id within that list.

**Parameters:**
  - `task_id` (string, required): The unique identifier of the Google Task to delete. Required - must be a task that exists within the specified tasklist_id. Obtain this ID by using the 'List Tasks' action with the corresponding tasklist_id. Multiple tasks may share the same title — verify against additional fields (e.g., due date, notes) before selecting the task_id to avoid deleting the wrong task.
  - `tasklist_id` (string, required): The unique identifier of the Google Task list containing the task to delete. Required - tasks cannot be deleted by task_id alone; the API requires specifying which list contains the task. Obtain this ID by using the 'List Task Lists' action.

### GOOGLETASKS_DELETE_TASK_LIST

**MCP Tool**: `mcp__composio__GOOGLETASKS_DELETE_TASK_LIST`

Permanently deletes an existing Google Task list, identified by `tasklist_id`, along with all its tasks; this operation is irreversible. Require explicit user confirmation before calling; do not invoke in read-only or exploratory flows.

**Parameters:**
  - `tasklist_id` (string, required): Unique identifier of the Google Task list to be deleted. This field is required and cannot be empty.

### GOOGLETASKS_GET_TASK

**MCP Tool**: `mcp__composio__GOOGLETASKS_GET_TASK`

Retrieve a specific Google Task. REQUIRES both `tasklist_id` and `task_id`. Tasks cannot be retrieved by ID alone - you must always specify which task list contains the task. Use this to refresh task details before display or edits rather than relying on potentially stale results from GOOGLETASKS_LIST_TASKS.

**Parameters:**
  - `task_id` (string, required): REQUIRED. Unique identifier of the Google Task to retrieve. Must be used together with tasklist_id.
  - `tasklist_id` (string, required): REQUIRED. Unique identifier of the Google Tasks list containing the task. You must always provide this parameter - tasks cannot be retrieved by task_id alone.

### GOOGLETASKS_GET_TASK_LIST

**MCP Tool**: `mcp__composio__GOOGLETASKS_GET_TASK_LIST`

Retrieves a specific task list from the user's Google Tasks if the `tasklist_id` exists for the authenticated user.

**Parameters:**
  - `tasklist_id` (string, required): The unique identifier of the task list to retrieve, assigned by Google Tasks when the list is created.

### GOOGLETASKS_INSERT_TASK

**MCP Tool**: `mcp__composio__GOOGLETASKS_INSERT_TASK`

Creates a new task in a given `tasklist_id`, optionally as a subtask of an existing `task_parent` or positioned after an existing `task_previous` sibling, where both `task_parent` and `task_previous` must belong to the same `tasklist_id` if specified. IMPORTANT: Date fields (due, completed) accept various formats like '28 Sep 2025', '11:59 PM, 22 Sep 2025', or ISO format '2025-09-21T15:30:00Z' and will automatically convert them to RFC3339 format required by the API. Not idempotent — repeated calls with identical parameters create duplicate tasks; track returned task IDs to avoid duplication. High-volume inserts may trigger 403 rateLimitExceeded or 429; apply exponential backoff.

**Parameters:**
  - `id` (string, optional): Unique identifier for the task. Generated by Google Tasks API when creating a task. Used for updating or deleting existing tasks.
  - `due` (string, optional): Due date for the task (optional). NOTE: Google Tasks only stores the date portion (YYYY-MM-DD) - any time component provided will be automatically stripped before sending to the API. 
Google Tasks API requires dates in RFC3339 format (e.g., 2025-09-22T13:48:27Z).
We automatically convert various human-readable formats to RFC3339:

Accepted formats:
• 2025-09-28T23:59:00Z → RFC3339/ISO-8601 format (preferred) - September 28, 2025 at 11:59 PM UTC
• 28 Sep 2025 → Simple date format - Will be converted to midnight UTC on that date
• 11:59 PM, 22 Sep 2025 → Time with date - Assumes UTC timezone if not specified
• 1:00 PM, 21 Sep 2025 → 12-hour format with date - Converted to 13:00 UTC
• UTC-5:30, 6:50 PM → With timezone offset - Converted to UTC automatically
• UTC+1, 11:59 PM, 31 Dec → Timezone with date - Next occurrence of Dec 31
• 2025-09-21T15:30:00+02:00 → RFC3339 with timezone offset

Note: If no timezone is specified, UTC is assumed. Dates without times default to midnight UTC.
  - `etag` (string, optional): ETag of the resource for concurrency control. Prevents accidental overwrites when multiple clients modify the same task. Auto-generated by the API.
  - `notes` (string, optional): Additional details or description for the task. Supports plain text only (no HTML/markdown). Maximum 8192 characters. Example: 'Include Q3 sales data and projections'
  - `title` (string, required): Title/name of the task (REQUIRED). This is the main text displayed for the task. Maximum 1024 characters. Example: 'Complete quarterly report'
  - `hidden` (boolean, optional): Whether the task is hidden from the default view. Hidden tasks can still be accessed via API but won't show in UI. Default: false
  - `status` (string, optional): Status of the task. Must be either 'needsAction' (task is pending) or 'completed' (task is done). Defaults to 'needsAction' if not provided.
  - `deleted` (boolean, optional): Whether the task has been deleted. Deleted tasks are marked but not immediately removed, allowing for recovery. Default: false
  - `completed` (string, optional): Date/time when the task was marked as completed. Only applicable when status='completed'. Unlike 'due', this field preserves the time component. 
Google Tasks API requires dates in RFC3339 format (e.g., 2025-09-22T13:48:27Z).
We automatically convert various human-readable formats to RFC3339:

Accepted formats:
• 2025-09-28T23:59:00Z → RFC3339/ISO-8601 format (preferred) - September 28, 2025 at 11:59 PM UTC
• 28 Sep 2025 → Simple date format - Will be converted to midnight UTC on that date
• 11:59 PM, 22 Sep 2025 → Time with date - Assumes UTC timezone if not specified
• 1:00 PM, 21 Sep 2025 → 12-hour format with date - Converted to 13:00 UTC
• UTC-5:30, 6:50 PM → With timezone offset - Converted to UTC automatically
• UTC+1, 11:59 PM, 31 Dec → Timezone with date - Next occurrence of Dec 31
• 2025-09-21T15:30:00+02:00 → RFC3339 with timezone offset

Note: If no timezone is specified, UTC is assumed. Dates without times default to midnight UTC.
  - `task_parent` (string, optional): Identifier of an existing task to serve as parent; if provided, creates a subtask, otherwise a top-level task in the specified list.
  - `tasklist_id` (string, required): Task list identifier where the new task will be created. Valid values: (1) '@default' - special identifier that references the user's default/primary task list, or (2) an actual task list ID obtained from the 'List Task Lists' action (GOOGLETASKS_LIST_TASK_LISTS). Task list IDs are opaque strings generated by Google (typically base64-encoded, e.g., 'MDQ1NTEzMjc4OTM5MzM0NTY4NzE6MDow').
  - `task_previous` (string, optional): Identifier of an existing task after which the new task will be placed, at the same hierarchical level (as a sibling).

### GOOGLETASKS_LIST_ALL_TASKS

**MCP Tool**: `mcp__composio__GOOGLETASKS_LIST_ALL_TASKS`

Tool to list all tasks across all of the user's task lists with optional filters. Use when the agent needs to see all tasks without knowing which list to query first. Each returned task is annotated with its tasklist_id and tasklist_title for context.

**Parameters:**
  - `dueMax` (string, optional): Only include tasks due on or before this date. 
Google Tasks API requires dates in RFC3339 format (e.g., 2025-09-22T13:48:27Z).
We automatically convert various human-readable formats to RFC3339:

Accepted formats:
• 2025-09-28T23:59:00Z → RFC3339/ISO-8601 format (preferred) - September 28, 2025 at 11:59 PM UTC
• 28 Sep 2025 → Simple date format - Will be converted to midnight UTC on that date
• 11:59 PM, 22 Sep 2025 → Time with date - Assumes UTC timezone if not specified
• 1:00 PM, 21 Sep 2025 → 12-hour format with date - Converted to 13:00 UTC
• UTC-5:30, 6:50 PM → With timezone offset - Converted to UTC automatically
• UTC+1, 11:59 PM, 31 Dec → Timezone with date - Next occurrence of Dec 31
• 2025-09-21T15:30:00+02:00 → RFC3339 with timezone offset

Note: If no timezone is specified, UTC is assumed. Dates without times default to midnight UTC.
  - `dueMin` (string, optional): Only include tasks due on or after this date. 
Google Tasks API requires dates in RFC3339 format (e.g., 2025-09-22T13:48:27Z).
We automatically convert various human-readable formats to RFC3339:

Accepted formats:
• 2025-09-28T23:59:00Z → RFC3339/ISO-8601 format (preferred) - September 28, 2025 at 11:59 PM UTC
• 28 Sep 2025 → Simple date format - Will be converted to midnight UTC on that date
• 11:59 PM, 22 Sep 2025 → Time with date - Assumes UTC timezone if not specified
• 1:00 PM, 21 Sep 2025 → 12-hour format with date - Converted to 13:00 UTC
• UTC-5:30, 6:50 PM → With timezone offset - Converted to UTC automatically
• UTC+1, 11:59 PM, 31 Dec → Timezone with date - Next occurrence of Dec 31
• 2025-09-21T15:30:00+02:00 → RFC3339 with timezone offset

Note: If no timezone is specified, UTC is assumed. Dates without times default to midnight UTC.
  - `showHidden` (boolean, optional): Include hidden tasks in results. Defaults to false.
  - `updatedMin` (string, optional): Only include tasks updated on or after this date. 
Google Tasks API requires dates in RFC3339 format (e.g., 2025-09-22T13:48:27Z).
We automatically convert various human-readable formats to RFC3339:

Accepted formats:
• 2025-09-28T23:59:00Z → RFC3339/ISO-8601 format (preferred) - September 28, 2025 at 11:59 PM UTC
• 28 Sep 2025 → Simple date format - Will be converted to midnight UTC on that date
• 11:59 PM, 22 Sep 2025 → Time with date - Assumes UTC timezone if not specified
• 1:00 PM, 21 Sep 2025 → 12-hour format with date - Converted to 13:00 UTC
• UTC-5:30, 6:50 PM → With timezone offset - Converted to UTC automatically
• UTC+1, 11:59 PM, 31 Dec → Timezone with date - Next occurrence of Dec 31
• 2025-09-21T15:30:00+02:00 → RFC3339 with timezone offset

Note: If no timezone is specified, UTC is assumed. Dates without times default to midnight UTC.
  - `showDeleted` (boolean, optional): Include deleted tasks in results. Defaults to false.
  - `completedMax` (string, optional): Only include tasks completed on or before this date. 
Google Tasks API requires dates in RFC3339 format (e.g., 2025-09-22T13:48:27Z).
We automatically convert various human-readable formats to RFC3339:

Accepted formats:
• 2025-09-28T23:59:00Z → RFC3339/ISO-8601 format (preferred) - September 28, 2025 at 11:59 PM UTC
• 28 Sep 2025 → Simple date format - Will be converted to midnight UTC on that date
• 11:59 PM, 22 Sep 2025 → Time with date - Assumes UTC timezone if not specified
• 1:00 PM, 21 Sep 2025 → 12-hour format with date - Converted to 13:00 UTC
• UTC-5:30, 6:50 PM → With timezone offset - Converted to UTC automatically
• UTC+1, 11:59 PM, 31 Dec → Timezone with date - Next occurrence of Dec 31
• 2025-09-21T15:30:00+02:00 → RFC3339 with timezone offset

Note: If no timezone is specified, UTC is assumed. Dates without times default to midnight UTC.
  - `completedMin` (string, optional): Only include tasks completed on or after this date. 
Google Tasks API requires dates in RFC3339 format (e.g., 2025-09-22T13:48:27Z).
We automatically convert various human-readable formats to RFC3339:

Accepted formats:
• 2025-09-28T23:59:00Z → RFC3339/ISO-8601 format (preferred) - September 28, 2025 at 11:59 PM UTC
• 28 Sep 2025 → Simple date format - Will be converted to midnight UTC on that date
• 11:59 PM, 22 Sep 2025 → Time with date - Assumes UTC timezone if not specified
• 1:00 PM, 21 Sep 2025 → 12-hour format with date - Converted to 13:00 UTC
• UTC-5:30, 6:50 PM → With timezone offset - Converted to UTC automatically
• UTC+1, 11:59 PM, 31 Dec → Timezone with date - Next occurrence of Dec 31
• 2025-09-21T15:30:00+02:00 → RFC3339 with timezone offset

Note: If no timezone is specified, UTC is assumed. Dates without times default to midnight UTC.
  - `showAssigned` (boolean, optional): Include tasks assigned to the current user in results. Defaults to false.
  - `showCompleted` (boolean, optional): Include completed tasks in results. Defaults to true.
  - `max_tasks_total` (integer, optional): Hard limit on total number of tasks returned across all lists to prevent large payloads. Defaults to 1000.

### GOOGLETASKS_LIST_TASK_LISTS

**MCP Tool**: `mcp__composio__GOOGLETASKS_LIST_TASK_LISTS`

Fetches the authenticated user's task lists from Google Tasks; results may be paginated. Response contains task lists under the `items` key. Multiple lists may share similar names — confirm the correct list by ID before passing to other tools.

**Parameters:**
  - `pageToken` (string, optional): Token for the page of results to return; omit for the first page, use `nextPageToken` from a previous response for subsequent pages.
  - `maxResults` (integer, optional): Maximum number of task lists to return per page. Capped at 100; values above 100 are silently truncated to 100.

### GOOGLETASKS_LIST_TASKS

**MCP Tool**: `mcp__composio__GOOGLETASKS_LIST_TASKS`

Retrieves tasks from a Google Tasks list; all date/time strings must be RFC3339 UTC, and `showCompleted` must be true if `completedMin` or `completedMax` are specified. Response key for tasks is `tasks` (not `items`). No full-text search; filter client-side by title/notes. Results ordered by position, not by date.

**Parameters:**
  - `dueMax` (string, optional): Exclude tasks due after this date. 
Google Tasks API requires dates in RFC3339 format (e.g., 2025-09-22T13:48:27Z).
We automatically convert various human-readable formats to RFC3339:

Accepted formats:
• 2025-09-28T23:59:00Z → RFC3339/ISO-8601 format (preferred) - September 28, 2025 at 11:59 PM UTC
• 28 Sep 2025 → Simple date format - Will be converted to midnight UTC on that date
• 11:59 PM, 22 Sep 2025 → Time with date - Assumes UTC timezone if not specified
• 1:00 PM, 21 Sep 2025 → 12-hour format with date - Converted to 13:00 UTC
• UTC-5:30, 6:50 PM → With timezone offset - Converted to UTC automatically
• UTC+1, 11:59 PM, 31 Dec → Timezone with date - Next occurrence of Dec 31
• 2025-09-21T15:30:00+02:00 → RFC3339 with timezone offset

Note: If no timezone is specified, UTC is assumed. Dates without times default to midnight UTC.
  - `dueMin` (string, optional): Exclude tasks due before this date. 
Google Tasks API requires dates in RFC3339 format (e.g., 2025-09-22T13:48:27Z).
We automatically convert various human-readable formats to RFC3339:

Accepted formats:
• 2025-09-28T23:59:00Z → RFC3339/ISO-8601 format (preferred) - September 28, 2025 at 11:59 PM UTC
• 28 Sep 2025 → Simple date format - Will be converted to midnight UTC on that date
• 11:59 PM, 22 Sep 2025 → Time with date - Assumes UTC timezone if not specified
• 1:00 PM, 21 Sep 2025 → 12-hour format with date - Converted to 13:00 UTC
• UTC-5:30, 6:50 PM → With timezone offset - Converted to UTC automatically
• UTC+1, 11:59 PM, 31 Dec → Timezone with date - Next occurrence of Dec 31
• 2025-09-21T15:30:00+02:00 → RFC3339 with timezone offset

Note: If no timezone is specified, UTC is assumed. Dates without times default to midnight UTC.
  - `pageToken` (string, optional): Token from a previous list operation for fetching a specific page; if omitted, retrieves the first page.
  - `maxResults` (integer, optional): Maximum number of tasks to return. API default: 20, maximum: 100. Use `nextPageToken` from the response in successive calls until absent to avoid missing tasks beyond the first page.
  - `showHidden` (boolean, optional): Include hidden tasks. Defaults to false.
  - `tasklistId` (string, required): Identifier of the task list. IMPORTANT: For the user's primary/default task list, you MUST use the literal string '@default' (NOT 'primary', 'default', or any other variation). The value 'primary' is NOT valid and will be rejected by the API. Either use '@default' for the primary list or provide a valid task list ID obtained from list_task_lists.
  - `updatedMin` (string, optional): Lower bound for task's last modification time (for syncing). 
Google Tasks API requires dates in RFC3339 format (e.g., 2025-09-22T13:48:27Z).
We automatically convert various human-readable formats to RFC3339:

Accepted formats:
• 2025-09-28T23:59:00Z → RFC3339/ISO-8601 format (preferred) - September 28, 2025 at 11:59 PM UTC
• 28 Sep 2025 → Simple date format - Will be converted to midnight UTC on that date
• 11:59 PM, 22 Sep 2025 → Time with date - Assumes UTC timezone if not specified
• 1:00 PM, 21 Sep 2025 → 12-hour format with date - Converted to 13:00 UTC
• UTC-5:30, 6:50 PM → With timezone offset - Converted to UTC automatically
• UTC+1, 11:59 PM, 31 Dec → Timezone with date - Next occurrence of Dec 31
• 2025-09-21T15:30:00+02:00 → RFC3339 with timezone offset

Note: If no timezone is specified, UTC is assumed. Dates without times default to midnight UTC.
  - `showDeleted` (boolean, optional): Include deleted tasks. Defaults to false.
  - `completedMax` (string, optional): Exclude tasks completed after this date. 
Google Tasks API requires dates in RFC3339 format (e.g., 2025-09-22T13:48:27Z).
We automatically convert various human-readable formats to RFC3339:

Accepted formats:
• 2025-09-28T23:59:00Z → RFC3339/ISO-8601 format (preferred) - September 28, 2025 at 11:59 PM UTC
• 28 Sep 2025 → Simple date format - Will be converted to midnight UTC on that date
• 11:59 PM, 22 Sep 2025 → Time with date - Assumes UTC timezone if not specified
• 1:00 PM, 21 Sep 2025 → 12-hour format with date - Converted to 13:00 UTC
• UTC-5:30, 6:50 PM → With timezone offset - Converted to UTC automatically
• UTC+1, 11:59 PM, 31 Dec → Timezone with date - Next occurrence of Dec 31
• 2025-09-21T15:30:00+02:00 → RFC3339 with timezone offset

Note: If no timezone is specified, UTC is assumed. Dates without times default to midnight UTC.
  - `completedMin` (string, optional): Exclude tasks completed before this date. 
Google Tasks API requires dates in RFC3339 format (e.g., 2025-09-22T13:48:27Z).
We automatically convert various human-readable formats to RFC3339:

Accepted formats:
• 2025-09-28T23:59:00Z → RFC3339/ISO-8601 format (preferred) - September 28, 2025 at 11:59 PM UTC
• 28 Sep 2025 → Simple date format - Will be converted to midnight UTC on that date
• 11:59 PM, 22 Sep 2025 → Time with date - Assumes UTC timezone if not specified
• 1:00 PM, 21 Sep 2025 → 12-hour format with date - Converted to 13:00 UTC
• UTC-5:30, 6:50 PM → With timezone offset - Converted to UTC automatically
• UTC+1, 11:59 PM, 31 Dec → Timezone with date - Next occurrence of Dec 31
• 2025-09-21T15:30:00+02:00 → RFC3339 with timezone offset

Note: If no timezone is specified, UTC is assumed. Dates without times default to midnight UTC.
  - `showCompleted` (boolean, optional): Include completed tasks. Defaults to true. (See action docstring for interaction with `completedMin`/`Max`).

### GOOGLETASKS_MOVE_TASK

**MCP Tool**: `mcp__composio__GOOGLETASKS_MOVE_TASK`

Moves the specified task to another position in the task list or to a different task list. Use cases: - Reorder tasks within a list (use 'previous' parameter) - Create subtasks by moving a task under a parent (use 'parent' parameter) - Move tasks between different task lists (use 'destinationTasklist' parameter) - Move a subtask back to top-level (omit 'parent' parameter)

**Parameters:**
  - `task` (string, required): Task identifier of the task to move. Must be a valid task ID from the source tasklist. Obtain this from 'List Tasks' or 'Insert Task' actions.
  - `parent` (string, optional): New parent task identifier. If provided, the task becomes a subtask of this parent. The parent must exist in the destination list (or source list if no destinationTasklist is specified). Omit to move task to top level.
  - `previous` (string, optional): New previous sibling task identifier. The task will be positioned immediately after this sibling. Omit to position the task first among its siblings. Must exist in the same list as the task's destination (destinationTasklist if specified, otherwise source tasklist). Cross-list sibling references are unsupported.
  - `tasklist` (string, required): Source task list identifier where the task currently exists. Use '@default' for the user's primary task list, or provide a specific task list ID obtained from 'List Task Lists'. The task must exist in this list.
  - `destinationTasklist` (string, optional): Destination task list identifier. If set, the task is moved from the source tasklist to this list. Omit to reorder within the same list. Note: Recurrent tasks cannot be moved between lists.

### GOOGLETASKS_PATCH_TASK

**MCP Tool**: `mcp__composio__GOOGLETASKS_PATCH_TASK`

Partially updates an existing task (identified by `task_id`) within a specific Google Task list (identified by `tasklist_id`), modifying only the provided attributes from `TaskInput` (e.g., `title`, `notes`, `due` date, `status`) and requiring both the task and list to exist.

**Parameters:**
  - `id` (string, optional): Unique identifier for the task. Generated by Google Tasks API when creating a task. Used for updating or deleting existing tasks.
  - `due` (string, optional): Due date for the task (optional). NOTE: Google Tasks only stores the date portion (YYYY-MM-DD) - any time component provided will be automatically stripped before sending to the API. Accepts RFC3339 format (e.g., '2025-09-28T23:59:00Z') or various human-readable formats like '28 Sep 2025', '11:59 PM, 22 Sep 2025'. Normalized to midnight UTC — naive local dates without timezone offset may shift ±1 day. Time-based reminders are not supported via this field.
  - `etag` (string, optional): ETag of the resource for concurrency control. Prevents accidental overwrites when multiple clients modify the same task. Auto-generated by the API.
  - `notes` (string, optional): Additional details or description for the task. Supports plain text only (no HTML/markdown). Maximum 8192 characters. Example: 'Include Q3 sales data and projections'
  - `title` (string, optional): Title/name of the task. This is the main text displayed for the task. Maximum 1024 characters. Example: 'Complete quarterly report'
  - `hidden` (boolean, optional): Whether the task is hidden from the default view. Hidden tasks can still be accessed via API but won't show in UI. Default: false
  - `status` (string, optional): Current status of the task. Must be either 'needsAction' (task is pending) or 'completed' (task is done).
  - `deleted` (boolean, optional): Whether the task has been deleted. Deleted tasks are marked but not immediately removed, allowing for recovery. Default: false
  - `task_id` (string, required): Identifier of the Google Task to be updated within the specified task list.
  - `completed` (string, optional): Date/time when the task was marked as completed. Only applicable when status='completed'. Unlike 'due', this field preserves the time component. Accepts RFC3339 format or human-readable date formats. Malformed or non-RFC3339 values will be rejected.
  - `tasklist_id` (string, required): Identifier of the Google Task list that contains the task to be updated.

### GOOGLETASKS_PATCH_TASK_LIST

**MCP Tool**: `mcp__composio__GOOGLETASKS_PATCH_TASK_LIST`

Updates the title of an existing Google Tasks task list.

**Parameters:**
  - `tasklist_id` (string, required): The unique identifier of the task list to be updated.
  - `updated_title` (string, optional): The new title for the task list. Optional for PATCH semantics - only include if you want to update the title. Maximum length: 1024 characters.

### GOOGLETASKS_UPDATE_TASK

**MCP Tool**: `mcp__composio__GOOGLETASKS_UPDATE_TASK`

DEPRECATED: Use GOOGLETASKS_PATCH_TASK instead. Full-update (PUT-style) operation that overwrites unspecified fields with empty/default values, which can cause data loss. Prefer GOOGLETASKS_PATCH_TASK unless a complete field replacement is explicitly required.

**Parameters:**
  - `due` (string, optional): Due date of the task (RFC 3339 timestamp).
  - `notes` (string, optional): Notes describing the task.
  - `title` (string, optional): Title of the task.
  - `status` (string, optional): Status of the task. Valid values: "needsAction" or "completed". When setting to "completed", the completion date will be auto-set to the current time if not explicitly provided via the completed field.
  - `task_id` (string, required): Task identifier.
  - `completed` (string, optional): Completion date of the task (RFC 3339 timestamp).
  - `tasklist_id` (string, required): Task list identifier.

### GOOGLETASKS_UPDATE_TASK_FULL

**MCP Tool**: `mcp__composio__GOOGLETASKS_UPDATE_TASK_FULL`

Tool to fully replace an existing Google Task using PUT method. Use when you need to update the entire task resource, not just specific fields. This method requires all required fields (id, title) and replaces the complete task, unlike PATCH which supports partial updates.

**Parameters:**
  - `id` (string, required): Task identifier (REQUIRED). Must match the task_id parameter. This field is required by the Google Tasks API for PUT operations.
  - `due` (string, optional): Due date for the task. NOTE: Google Tasks only stores the date portion (YYYY-MM-DD). Accepts RFC3339 format (e.g., '2025-09-28T00:00:00Z') or date strings like '2025-09-28'.
  - `etag` (string, optional): ETag of the resource for concurrency control. Prevents accidental overwrites when multiple clients modify the same task.
  - `links` (array, optional): Collection of links attached to the task. This collection is read-only.
  - `notes` (string, optional): Additional details or description for the task. Supports plain text only. Maximum 8192 characters.
  - `title` (string, required): Title/name of the task (REQUIRED). This is the main text displayed for the task. Maximum 1024 characters.
  - `hidden` (boolean, optional): Whether the task is hidden. This is read-only and set by the API when task list is cleared. Default: false
  - `parent` (string, optional): Parent task identifier for subtasks. This field is read-only. Use the 'move' method to change parent.
  - `status` (string, optional): Current status of the task. Must be either 'needsAction' (task is pending) or 'completed' (task is done).
  - `deleted` (boolean, optional): Whether the task has been deleted. Deleted tasks are marked but not immediately removed. Default: false
  - `task_id` (string, required): Identifier of the Google Task to be fully replaced within the specified task list. This must also be included in the 'id' field of the request body.
  - `position` (string, optional): Position of the task among siblings. This field is read-only. Use the 'move' method to change position.
  - `completed` (string, optional): Date/time when the task was marked as completed. Only applicable when status='completed'. Unlike 'due', this field preserves the time component. Accepts RFC3339 format.
  - `tasklist_id` (string, required): Identifier of the Google Task list that contains the task to be updated.

### GOOGLETASKS_UPDATE_TASK_LIST

**MCP Tool**: `mcp__composio__GOOGLETASKS_UPDATE_TASK_LIST`

Updates the authenticated user's specified task list.

**Parameters:**
  - `title` (string, required): Title of the task list. Maximum length allowed: 1024 characters.
  - `tasklist_id` (string, required): Task list identifier.


## Usage Examples

#### GOOGLETASKS_BATCH_EXECUTE
```
Use MCP tool: mcp__composio__GOOGLETASKS_BATCH_EXECUTE
Parameters: {
  "requests": "value"
}
```

#### GOOGLETASKS_BULK_INSERT_TASKS
```
Use MCP tool: mcp__composio__GOOGLETASKS_BULK_INSERT_TASKS
Parameters: {
  "tasklist_id": "your_tasklist_id_here",
  "tasks": "value"
}
```

#### GOOGLETASKS_CLEAR_TASKS
```
Use MCP tool: mcp__composio__GOOGLETASKS_CLEAR_TASKS
Parameters: {
  "tasklist": "your_tasklist_here"
}
```

## Quick Reference

| MCP Tool | Description |
|----------|-------------|
| `mcp__composio__GOOGLETASKS_BATCH_EXECUTE` | Executes multiple Google Tasks API operations in a... |
| `mcp__composio__GOOGLETASKS_BULK_INSERT_TASKS` | DEPRECATED: Use BatchExecute instead. Creates mult... |
| `mcp__composio__GOOGLETASKS_CLEAR_TASKS` | Permanently and irreversibly clears all completed ... |
| `mcp__composio__GOOGLETASKS_CREATE_TASK_LIST` | Creates a new task list with the specified title a... |
| `mcp__composio__GOOGLETASKS_DELETE_TASK` | Deletes a specified task from a Google Tasks list.... |
| `mcp__composio__GOOGLETASKS_DELETE_TASK_LIST` | Permanently deletes an existing Google Task list, ... |
| `mcp__composio__GOOGLETASKS_GET_TASK` | Retrieve a specific Google Task. REQUIRES both `ta... |
| `mcp__composio__GOOGLETASKS_GET_TASK_LIST` | Retrieves a specific task list from the user's Goo... |
| `mcp__composio__GOOGLETASKS_INSERT_TASK` | Creates a new task in a given `tasklist_id`, optio... |
| `mcp__composio__GOOGLETASKS_LIST_ALL_TASKS` | Tool to list all tasks across all of the user's ta... |
| `mcp__composio__GOOGLETASKS_LIST_TASK_LISTS` | Fetches the authenticated user's task lists from G... |
| `mcp__composio__GOOGLETASKS_LIST_TASKS` | Retrieves tasks from a Google Tasks list; all date... |
| `mcp__composio__GOOGLETASKS_MOVE_TASK` | Moves the specified task to another position in th... |
| `mcp__composio__GOOGLETASKS_PATCH_TASK` | Partially updates an existing task (identified by ... |
| `mcp__composio__GOOGLETASKS_PATCH_TASK_LIST` | Updates the title of an existing Google Tasks task... |
| `mcp__composio__GOOGLETASKS_UPDATE_TASK` | DEPRECATED: Use GOOGLETASKS_PATCH_TASK instead. Fu... |
| `mcp__composio__GOOGLETASKS_UPDATE_TASK_FULL` | Tool to fully replace an existing Google Task usin... |
| `mcp__composio__GOOGLETASKS_UPDATE_TASK_LIST` | Updates the authenticated user's specified task li... |

## When to Use This Integration

**Use Google Tasks when:**
- Interacting with Google Tasks services
- Automating Google Tasks workflows
- Reading and writing Google Tasks data

## Best Practices

1. **Prefer batch operations** when working with multiple items
2. **Check permissions** before performing write operations
3. **Handle errors gracefully** - MCP returns structured error responses
4. **Use appropriate tools** - choose the most specific tool for the task

## Authentication

- **Method**: OAuth via Composio
- **Status**: Connected and managed by Composio
- **Token Refresh**: Automatic

## Troubleshooting

If MCP tools are not available:
1. Check that `.mcp.json` is properly configured
2. Verify Composio API key is valid
3. Ensure the integration is connected in Composio dashboard
4. Restart Claude Code to reload MCP configuration

## Related Skills

Check Composio dashboard for related integrations.
