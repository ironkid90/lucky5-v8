---
name: outlook
description: Outlook is Microsoft's email and calendaring platform integrating contacts and scheduling, enabling users to manage communications and events in a unified workspace integration via Composio MCP. Use when user needs to interact with Outlook.
---

# Outlook Integration

> Auto-generated by Composio Skills Extension on 2026-07-11T01:11:49.340Z
> **Execution**: Via Composio MCP Server (native Claude Code integration)

## Overview

Outlook is Microsoft's email and calendaring platform integrating contacts and scheduling, enabling users to manage communications and events in a unified workspace

This skill uses **Composio MCP** for tool execution. Tools are available as native MCP tools
that Claude can call directly without any Bash commands or scripts.

## MCP Configuration

This integration requires Composio MCP server to be configured in `.mcp.json`:

```json
{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": ["-y", "@composio/mcp"],
      "env": {
        "COMPOSIO_API_KEY": "your_api_key",
        "COMPOSIO_ENTITY_ID": "your_entity_id"
      }
    }
  }
}
```

## Available MCP Tools

### OUTLOOK_ACCEPT_EVENT

**MCP Tool**: `mcp__composio__OUTLOOK_ACCEPT_EVENT`

Accepts or tentatively accepts a calendar meeting invite on behalf of a user. Use this action when a user has received a meeting invitation and wants to indicate their attendance status (either confirmed or tentative). The organizer will receive a notification of the acceptance unless send_response is set to false.

**Parameters:**
  - `comment` (string, optional): Optional text message to include with the response, sent to the organizer.
  - `user_id` (string, optional): The user ID or userPrincipalName of the calendar owner. If not provided, uses the authenticated user (/me).
  - `event_id` (string, required): The unique identifier of the calendar event to respond to.
  - `response_type` (string, optional): Type of response: 'accept' to accept the invite, 'tentative' to tentatively accept.
  - `send_response` (boolean, optional): Whether to send the response to the organizer. Set to false to accept without notifying.

### OUTLOOK_ADD_EVENT_ATTACHMENT

**MCP Tool**: `mcp__composio__OUTLOOK_ADD_EVENT_ATTACHMENT`

Adds an attachment to a specific Outlook calendar event. Use when you need to attach a file or nested item to an existing event.

**Parameters:**
  - `item` (object, optional): The nested item payload; required when '@odata.type' is itemAttachment.
  - `name` (string, required): The display name of the attachment.
  - `user_id` (string, optional): The user's email address or 'me' for the authenticated user.
  - `event_id` (string, required): The unique opaque identifier of the calendar event (NOT an email address). This is typically a long string starting with 'AAMkA' or 'AQMkA'. Obtain it from listing or searching calendar events.
  - `odata_type` (string, required): Attachment type: '#microsoft.graph.fileAttachment' requires 'contentBytes'; '#microsoft.graph.itemAttachment' requires 'item'.
  - `content_bytes` (string, optional): MUST be base64-encoded file contents (not plain text); required when '@odata.type' is fileAttachment. Raw/plain text must be base64-encoded first.

### OUTLOOK_ADD_MAIL_ATTACHMENT

**MCP Tool**: `mcp__composio__OUTLOOK_ADD_MAIL_ATTACHMENT`

Tool to add an attachment to an email message. Use when you have a message ID and need to attach a small (<3 MB) file or reference.

**Parameters:**
  - `item` (object, optional): Embedded item (e.g., message, event) for itemAttachment; must include its own '@odata.type'.
  - `name` (string, optional): Display name of the attachment (e.g., file name). Required when using content_bytes. When using the attachment field, the name is automatically inferred.
  - `user_id` (string, optional): The user's principal name or 'me' for the authenticated user.
  - `isInline` (boolean, optional): Set to true if the attachment should appear inline in the message body.
  - `contentId` (string, optional): Content ID for inline attachments, used in HTML body references.
  - `attachment` (string, optional): File to attach to the email. Either provide this field OR use 'content_bytes'. Cannot be used together with 'content_bytes'.
  - `message_id` (string, required): Unique Microsoft Graph message ID (opaque string like 'AAMkAGI2TAAA='). NOT an email address - this is an internal identifier returned by Microsoft Graph API operations.
  - `odata_type` (string, optional): The OData type of the attachment. Required by Microsoft Graph API. Use '#microsoft.graph.fileAttachment' for file attachments, '#microsoft.graph.itemAttachment' for embedded items (messages, events, contacts).
  - `contentType` (string, optional): MIME type of the attachment (e.g., 'application/pdf'). Required when using content_bytes. When using the attachment field, the mimetype is automatically inferred.
  - `contentBytes` (string, optional): Base64-encoded content of the file attachment (max size 3 MB). Either provide this field OR use the 'attachment' field (recommended). Cannot be used together with 'attachment'.
  - `contentLocation` (string, optional): URL location or path for reference attachments.

### OUTLOOK_BATCH_MOVE_MESSAGES

**MCP Tool**: `mcp__composio__OUTLOOK_BATCH_MOVE_MESSAGES`

Batch-move up to 20 Outlook messages to a destination folder in a single Microsoft Graph $batch call. Use when moving multiple messages to avoid per-message move API calls.

**Parameters:**
  - `user_id` (string, optional): The UPN (User Principal Name) of the user whose messages to move, or 'me' for the currently authenticated user.
  - `message_ids` (array, required): Array of message IDs to move. Maximum 20 items per batch (Microsoft Graph API limit). Each ID must be complete and untruncated.
  - `destination_id` (string, required): The destination folder ID, or a well-known folder name (e.g., 'inbox', 'deleteditems', 'drafts', 'sentitems'). If using a folder ID, it must be complete and untruncated.
  - `response_detail` (string, optional): Controls how much each move sub-request returns from Microsoft Graph. 'minimal' (default) sets `Prefer: return=minimal` on each sub-request, so Graph returns 204 No Content per move — the response stays small even for the maximum 20 HTML messages. In this mode `moved_message_id` in each result is null even on success. 'full' omits the Prefer header so Graph returns the full moved-message body per sub-request and `moved_message_id` is populated. Use 'full' if you need to chain follow-up calls on the new message ID; otherwise stick with 'minimal' to avoid multi-MB batch responses.

### OUTLOOK_BATCH_UPDATE_MESSAGES

**MCP Tool**: `mcp__composio__OUTLOOK_BATCH_UPDATE_MESSAGES`

Batch-update up to 20 Outlook messages per call using Microsoft Graph JSON batching. Use when marking multiple messages read/unread or updating other properties to avoid per-message PATCH calls.

**Parameters:**
  - `updates` (array, required): Array of message updates to perform. Maximum 20 items per batch (Microsoft Graph API limit). For larger workloads, chunk into multiple batch calls and retry any failures.
  - `user_id` (string, optional): The UPN (User Principal Name) of the user whose messages to update, or 'me' for the currently authenticated user.
  - `response_detail` (string, optional): Controls how much each PATCH sub-request returns from Microsoft Graph. 'minimal' (default) sets `Prefer: return=minimal` on each sub-request, so Graph returns 204 No Content per update — the response stays small even for the maximum 20 HTML messages. The action's response shape (BatchItemResult.status / .success / .error_message) is unchanged. 'full' omits the Prefer header so Graph returns the full updated-message body per sub-request (status 200) — useful only if you want to inspect the raw Graph response for debugging; otherwise stick with 'minimal' to avoid multi-MB batch responses.

### OUTLOOK_CALENDAR_CREATE_EVENT

**MCP Tool**: `mcp__composio__OUTLOOK_CALENDAR_CREATE_EVENT`

Creates a new Outlook calendar event, ensuring `start_datetime` is chronologically before `end_datetime`.

**Parameters:**
  - `body` (string, optional): The body of the event. This can be in plain text or HTML format, as specified by the 'is_html' field. Optional - events can be created without a body.
  - `is_html` (boolean, optional): Specifies whether the 'body' content is HTML. Set to true for HTML content; otherwise, it's treated as plain text.
  - `show_as` (string, optional): The status to show on the calendar for the duration of the event. Valid values are: 'free', 'tentative', 'busy', 'oof' (out of office), 'workingElsewhere', 'unknown'.
  - `subject` (string, required): The subject of the calendar event.
  - `user_id` (string, optional): The user identifier for the calendar owner. Must be either 'me' (for the authenticated user), a Microsoft 365 User Principal Name (e.g., user@contoso.com - must be a work/school account in the organization's verified domain), or an Azure AD object ID (GUID). Personal email addresses (Gmail, Yahoo, Outlook.com consumer accounts, etc.) are NOT valid and will result in a 404 error.
  - `location` (string, optional): The physical location of the event.
  - `time_zone` (string, required): The time zone for the start and end times of the event. Uses Windows time zone names (e.g., 'Pacific Standard Time') or IANA time zone names (e.g., 'America/Los_Angeles'). UTC is also a valid input.
  - `categories` (array, optional): A list of category names to associate with the event. The API accepts any string values. For best compatibility with Outlook UI color-coding, use category names from the user's master category list. Category names are case-insensitive and duplicates will be automatically removed.
  - `importance` (string, optional): The importance of the event. Valid values are 'low', 'normal', 'high'. Defaults to 'normal'.
  - `recurrence` (object, optional): The recurrence pattern and range for an event.
  - `calendar_id` (string, optional): The ID of a specific calendar to create the event in. Obtainable from the list calendars action. If omitted, the event is created on the user's default calendar.
  - `end_datetime` (string, required): The end date and time of the event in ISO 8601 format. Time zone is specified in 'time_zone'.
  - `attendees_info` (array, optional): A list of attendees for the event. Accepts multiple formats: (1) Simple email strings (e.g., ['user@example.com']), (2) Attendee objects with 'email' (required), 'name' (optional), and 'type' (optional: 'required', 'optional', 'resource', defaults to 'required'), (3) Microsoft Graph-style objects with nested 'emailAddress' containing 'address' and 'name' fields.
  - `start_datetime` (string, required): The start date and time of the event in ISO 8601 format. Time zone is specified in 'time_zone'.
  - `is_online_meeting` (boolean, optional): Set to true to indicate that the event is an online meeting. This will automatically generate an online meeting link if 'online_meeting_provider' is set (e.g., a Microsoft Teams link).
  - `online_meeting_provider` (string, optional): Specifies the online meeting provider. Currently, only 'teamsForBusiness' is supported. If 'is_online_meeting' is true and this is set, a meeting link for the provider will be created.

### OUTLOOK_CANCEL_CALENDAR_EVENT

**MCP Tool**: `mcp__composio__OUTLOOK_CANCEL_CALENDAR_EVENT`

Tool to cancel an event in a specific calendar for a specified user and send cancellation notifications to all attendees. Use when you need to cancel a meeting or event in a specific calendar on behalf of a user.

**Parameters:**
  - `Comment` (string, optional): Optional text message sent to all attendees explaining the cancellation.
  - `user_id` (string, required): The user ID or userPrincipalName of the calendar owner. Use 'me' for the authenticated user.
  - `event_id` (string, required): The unique identifier of the event to cancel.
  - `calendar_id` (string, required): The unique identifier of the calendar containing the event.

### OUTLOOK_CANCEL_CALENDAR_GROUP_CALENDAR_EVENT

**MCP Tool**: `mcp__composio__OUTLOOK_CANCEL_CALENDAR_GROUP_CALENDAR_EVENT`

Tool to cancel an event in a user's calendar within a calendar group and send cancellation notifications to all attendees. Use when canceling a meeting or event for a specific user in a calendar that belongs to a calendar group.

**Parameters:**
  - `comment` (string, optional): Optional text message sent to all attendees explaining the cancellation.
  - `user_id` (string, required): The user ID or userPrincipalName of the calendar owner. Can be 'me' for the authenticated user or a specific user identifier.
  - `event_id` (string, required): The unique identifier of the event to cancel.
  - `calendar_id` (string, required): The ID of the calendar within the calendar group.
  - `calendar_group_id` (string, required): The ID of the calendar group containing the target calendar.

### OUTLOOK_CANCEL_EVENT

**MCP Tool**: `mcp__composio__OUTLOOK_CANCEL_EVENT`

Tool to cancel a calendar event for a specified user and send cancellation notifications to all attendees. Use when you need to cancel a meeting or event on behalf of a specific user.

**Parameters:**
  - `Comment` (string, optional): Optional text message sent to all attendees explaining the cancellation.
  - `user_id` (string, required): The user ID or userPrincipalName of the calendar owner. Use 'me' for the authenticated user.
  - `event_id` (string, required): The unique identifier of the calendar event to cancel.

### OUTLOOK_COPY_MAIL_FOLDER

**MCP Tool**: `mcp__composio__OUTLOOK_COPY_MAIL_FOLDER`

Tool to copy a user's mail folder and its contents to another folder. Use when you need to duplicate a folder structure for a specific user's mailbox.

**Parameters:**
  - `user_id` (string, required): Identifier of the user. Can be the user's ID, userPrincipalName (email address), or 'me' for the authenticated user.
  - `destination_id` (string, required): The folder ID or a well-known folder name where the folder should be copied to. The copied folder will be placed in this destination.
  - `mail_folder_id` (string, required): The ID of the mail folder to copy. Can be a folder ID or a well-known folder name (e.g., 'inbox', 'drafts', 'sentitems').

### OUTLOOK_COPY_ME_MAIL_FOLDER

**MCP Tool**: `mcp__composio__OUTLOOK_COPY_ME_MAIL_FOLDER`

Tool to copy a child mail folder to a destination folder. Use when you need to duplicate a folder structure.

**Parameters:**
  - `user_id` (string, optional): User's id, userPrincipalName, or 'me' for the signed-in user.
  - `destination_id` (string, required): The folder ID of the destination folder, or a well-known folder name. You can provide either the folder's Microsoft Graph ID (e.g., 'AAMkAGI0ZjExAAA=') or use a well-known name like 'inbox', 'drafts', 'sentitems', 'deleteditems', etc.
  - `mail_folder_id` (string, required): The ID of the parent mail folder containing the child folder to copy.
  - `child_folder_id` (string, required): The ID of the child folder to copy.

### OUTLOOK_COPY_MESSAGE

**MCP Tool**: `mcp__composio__OUTLOOK_COPY_MESSAGE`

Tool to copy an email message to another folder within the user's mailbox. Use when duplicating messages to multiple folders for organization.

**Parameters:**
  - `user_id` (string, optional): User's email address, UPN, or 'me' for the currently authenticated user.
  - `message_id` (string, required): Unique ID of the Outlook email message to copy. Must be the complete, untruncated ID string - do not use shortened or ellipsis-containing IDs.
  - `destination_id` (string, required): The destination folder ID, or a well-known folder name (e.g., 'inbox', 'deleteditems', 'drafts', 'sentitems'). If using a folder ID, it must be the complete, untruncated ID string - do not use shortened or ellipsis-containing IDs.

### OUTLOOK_COPY_MESSAGE_FROM_CHILD_FOLDER

**MCP Tool**: `mcp__composio__OUTLOOK_COPY_MESSAGE_FROM_CHILD_FOLDER`

Tool to copy an email message from a child folder (nested folder) to another folder within the user's mailbox. Use when duplicating messages from nested folder structures.

**Parameters:**
  - `user_id` (string, optional): User's email address, UPN, user object ID, or 'me' for the currently authenticated user.
  - `message_id` (string, required): Unique ID of the Outlook email message to copy. Must be the complete, untruncated ID string - do not use shortened or ellipsis-containing IDs.
  - `destinationId` (string, required): The destination folder ID, or a well-known folder name (e.g., 'inbox', 'deleteditems', 'drafts', 'sentitems', 'junkemail'). If using a folder ID, it must be the complete, untruncated ID string - do not use shortened or ellipsis-containing IDs.
  - `mail_folder_id` (string, required): The ID or well-known name of the parent mail folder. Common well-known names include: 'inbox', 'drafts', 'sentitems', 'deleteditems', 'junkemail', 'archive', 'outbox', 'clutter', 'conflicts', 'conversationhistory', 'localfailures', 'msgfolderroot', 'recoverableitemsdeletions', 'scheduled', 'searchfolders', 'serverfailures', 'syncissues'. For folder IDs, obtain them from OUTLOOK_LIST_MAIL_FOLDERS. Folder IDs are base64-encoded strings (e.g., 'AAMkAGI0ZjExAAA=').
  - `child_folder_id` (string, required): The ID of the child folder containing the message. Obtain this from OUTLOOK_LIST_CHILD_MAIL_FOLDERS. Child folder IDs are base64-encoded strings (e.g., 'AAMkADAwATMwMAExAAA=').

### OUTLOOK_COPY_MESSAGE_FROM_MAIL_FOLDER

**MCP Tool**: `mcp__composio__OUTLOOK_COPY_MESSAGE_FROM_MAIL_FOLDER`

Tool to copy a message from a specific user's mail folder to another folder. Use when you need to duplicate a message from a known source folder to a destination folder for a specific user.

**Parameters:**
  - `user_id` (string, optional): User's email address, UPN, or 'me' for the currently authenticated user.
  - `message_id` (string, required): Unique ID of the Outlook email message to copy. Must be the complete, untruncated ID string - do not use shortened or ellipsis-containing IDs.
  - `destinationId` (string, required): The destination folder ID, or a well-known folder name (e.g., 'inbox', 'deleteditems', 'drafts', 'sentitems'). If using a folder ID, it must be the complete, untruncated ID string - do not use shortened or ellipsis-containing IDs.
  - `mail_folder_id` (string, required): The ID of the source mail folder containing the message. Must be the complete, untruncated ID string - do not use shortened or ellipsis-containing IDs. Can also use well-known folder names (e.g., 'inbox', 'drafts', 'sentitems').

### OUTLOOK_CREATE_ATTACHMENT_UPLOAD_SESSION

**MCP Tool**: `mcp__composio__OUTLOOK_CREATE_ATTACHMENT_UPLOAD_SESSION`

Tool to create an upload session for large (>3 MB) message attachments. Use when you need to upload attachments in chunks.

**Parameters:**
  - `user_id` (string, optional): User ID or user principal name; use 'me' for the signed-in user.
  - `message_id` (string, required): The unique identifier of the message to attach to.
  - `attachment_item` (object, required): AttachmentItem object describing the attachment in the upload session.

### OUTLOOK_CREATE_ATTACHMENT_UPLOAD_SESSION_IN_CHILD_FOLDER

**MCP Tool**: `mcp__composio__OUTLOOK_CREATE_ATTACHMENT_UPLOAD_SESSION_IN_CHILD_FOLDER`

Tool to create an upload session for large (>3 MB) message attachments in child mail folders. Use when you need to upload attachments to messages in nested folder structures.

**Parameters:**
  - `user_id` (string, optional): User ID or user principal name; use 'me' for the signed-in user.
  - `message_id` (string, required): The unique identifier of the message to attach to.
  - `mail_folder_id` (string, required): The ID or well-known name of the parent mail folder. ONLY these specific well-known names are valid as string names: 'inbox', 'drafts', 'sentitems', 'deleteditems', 'junkemail', 'archive', 'outbox', 'clutter', 'conflicts', 'conversationhistory', 'localfailures', 'msgfolderroot', 'recoverableitemsdeletions', 'scheduled', 'searchfolders', 'serverfailures', 'syncissues'. If the folder is not one of these well-known names, you MUST use the actual folder ID - a base64-encoded string (e.g., 'AAMkAGI0ZjExAAA=').
  - `attachment_item` (object, required): AttachmentItem object describing the attachment in the upload session.
  - `child_folder_id` (string, required): The unique identifier of the child mail folder. This is a base64-encoded string (e.g., 'AAMkAGI0ZjExAAA=').

### OUTLOOK_CREATE_CALENDAR

**MCP Tool**: `mcp__composio__OUTLOOK_CREATE_CALENDAR`

Tool to create a new calendar in the signed-in user's mailbox. Use when organizing events into a separate calendar.

**Parameters:**
  - `name` (string, required): The display name of the new calendar.
  - `color` (string, optional): The theme color to assign to the calendar. Supported values: auto, lightBlue, lightGreen, lightOrange, lightGray, lightYellow, lightTeal, lightPink, lightBrown, lightPurple, lightRed.
  - `user_id` (string, optional): The UPN (User Principal Name) or object ID of the user where the calendar will be created. Use 'me' for the signed-in user.
  - `hex_color` (string, optional): An optional hexadecimal color code for the calendar in the format '#RRGGBB'.

### OUTLOOK_CREATE_CALENDAR_EVENT

**MCP Tool**: `mcp__composio__OUTLOOK_CREATE_CALENDAR_EVENT`

DEPRECATED: Use OUTLOOK_CREATE_USER_CALENDAR_CALENDAR_EVENT instead. Tool to create a new event in a specific Outlook calendar. Use when you need to add an event to a particular calendar by its ID.

**Parameters:**
  - `body` (string, optional): The body of the event. This can be in plain text or HTML format, as specified by the 'is_html' field. Optional - events can be created without a body.
  - `is_html` (boolean, optional): Specifies whether the 'body' content is HTML. Set to true for HTML content; otherwise, it's treated as plain text.
  - `show_as` (string, optional): The status to show on the calendar for the duration of the event. Valid values are: 'free', 'tentative', 'busy', 'oof' (out of office), 'workingElsewhere', 'unknown'.
  - `subject` (string, required): The subject of the calendar event.
  - `user_id` (string, optional): The user ID (GUID) or user principal name (email) of the user. Required for S2S (app-only) authentication. If not provided, defaults to the authenticated user (/me endpoint).
  - `location` (string, optional): The physical location of the event.
  - `time_zone` (string, required): The time zone for the start and end times of the event. Uses Windows time zone names (e.g., 'Pacific Standard Time') or IANA time zone names (e.g., 'America/Los_Angeles'). UTC is also a valid input.
  - `categories` (array, optional): A list of category names to associate with the event. The API accepts any string values. For best compatibility with Outlook UI color-coding, use category names from the user's master category list. Category names are case-insensitive and duplicates will be automatically removed.
  - `importance` (string, optional): The importance of the event. Valid values are 'low', 'normal', 'high'. Defaults to 'normal'.
  - `recurrence` (object, optional): The recurrence pattern and range for an event.
  - `calendar_id` (string, required): The ID of the calendar where the event will be created. Use calendar ID from the listCalendars action.
  - `end_datetime` (string, required): The end date and time of the event in ISO 8601 format. Time zone is specified in 'time_zone'.
  - `attendees_info` (array, optional): A list of attendees for the event. Accepts multiple formats: (1) Simple email strings (e.g., ['user@example.com']), (2) Attendee objects with 'email' (required), 'name' (optional), and 'type' (optional: 'required', 'optional', 'resource', defaults to 'required'), (3) Microsoft Graph-style objects with nested 'emailAddress' containing 'address' and 'name' fields.
  - `start_datetime` (string, required): The start date and time of the event in ISO 8601 format. Time zone is specified in 'time_zone'.
  - `is_online_meeting` (boolean, optional): Set to true to indicate that the event is an online meeting. This will automatically generate an online meeting link if 'online_meeting_provider' is set (e.g., a Microsoft Teams link).
  - `online_meeting_provider` (string, optional): Specifies the online meeting provider. Currently, only 'teamsForBusiness' is supported. If 'is_online_meeting' is true and this is set, a meeting link for the provider will be created.

### OUTLOOK_CREATE_CALENDAR_EVENT_ATTACHMENT

**MCP Tool**: `mcp__composio__OUTLOOK_CREATE_CALENDAR_EVENT_ATTACHMENT`

Tool to create a new attachment for an event in a specific calendar. Use when you need to attach a file or item to an event within a particular calendar.

**Parameters:**
  - `item` (object, optional): The nested item payload; required when '@odata.type' is itemAttachment.
  - `name` (string, required): The display name of the attachment.
  - `event_id` (string, required): The unique opaque identifier of the calendar event (NOT an email address). This is typically a long string starting with 'AAMkA' or 'AQMkA'. Obtain it from listing or searching calendar events.
  - `odata_type` (string, required): Attachment type: '#microsoft.graph.fileAttachment' requires 'contentBytes'; '#microsoft.graph.itemAttachment' requires 'item'.
  - `calendar_id` (string, required): The unique identifier of the calendar. This is typically a long string identifying the specific calendar.
  - `contentBytes` (string, optional): MUST be base64-encoded file contents (not plain text); required when '@odata.type' is fileAttachment. Raw/plain text must be base64-encoded first.

### OUTLOOK_CREATE_CALENDAR_EVENT_ATTACHMENT_UPLOAD_SESSION

**MCP Tool**: `mcp__composio__OUTLOOK_CREATE_CALENDAR_EVENT_ATTACHMENT_UPLOAD_SESSION`

Tool to create an upload session for large calendar event attachments in a specific calendar. Use when attaching files larger than 3 MB to Outlook calendar events in a specific calendar.

**Parameters:**
  - `user_id` (string, optional): The user ID (GUID) or user principal name (email) whose calendar contains the event. Required for S2S (app-only) authentication. If not provided, uses the authenticated user context (/me endpoint).
  - `event_id` (string, required): The unique identifier of the calendar event to attach the file to. Obtain from listing or searching calendar events.
  - `calendar_id` (string, required): The unique identifier of the calendar containing the event. Obtain from listing calendars.
  - `attachment_item` (object, required): AttachmentItem object specifying the type, name, and size of the attachment to upload.


## Usage Examples

#### OUTLOOK_ACCEPT_EVENT
```
Use MCP tool: mcp__composio__OUTLOOK_ACCEPT_EVENT
Parameters: {
  "event_id": "your_event_id_here"
}
```

#### OUTLOOK_ADD_EVENT_ATTACHMENT
```
Use MCP tool: mcp__composio__OUTLOOK_ADD_EVENT_ATTACHMENT
Parameters: {
  "event_id": "your_event_id_here",
  "odata_type": "your_odata_type_here"
}
```

#### OUTLOOK_ADD_MAIL_ATTACHMENT
```
Use MCP tool: mcp__composio__OUTLOOK_ADD_MAIL_ATTACHMENT
Parameters: {
  "message_id": "your_message_id_here"
}
```

## Quick Reference

| MCP Tool | Description |
|----------|-------------|
| `mcp__composio__OUTLOOK_ACCEPT_EVENT` | Accepts or tentatively accepts a calendar meeting ... |
| `mcp__composio__OUTLOOK_ADD_EVENT_ATTACHMENT` | Adds an attachment to a specific Outlook calendar ... |
| `mcp__composio__OUTLOOK_ADD_MAIL_ATTACHMENT` | Tool to add an attachment to an email message. Use... |
| `mcp__composio__OUTLOOK_BATCH_MOVE_MESSAGES` | Batch-move up to 20 Outlook messages to a destinat... |
| `mcp__composio__OUTLOOK_BATCH_UPDATE_MESSAGES` | Batch-update up to 20 Outlook messages per call us... |
| `mcp__composio__OUTLOOK_CALENDAR_CREATE_EVENT` | Creates a new Outlook calendar event, ensuring `st... |
| `mcp__composio__OUTLOOK_CANCEL_CALENDAR_EVENT` | Tool to cancel an event in a specific calendar for... |
| `mcp__composio__OUTLOOK_CANCEL_CALENDAR_GROUP_CALENDAR_EVENT` | Tool to cancel an event in a user's calendar withi... |
| `mcp__composio__OUTLOOK_CANCEL_EVENT` | Tool to cancel a calendar event for a specified us... |
| `mcp__composio__OUTLOOK_COPY_MAIL_FOLDER` | Tool to copy a user's mail folder and its contents... |
| `mcp__composio__OUTLOOK_COPY_ME_MAIL_FOLDER` | Tool to copy a child mail folder to a destination ... |
| `mcp__composio__OUTLOOK_COPY_MESSAGE` | Tool to copy an email message to another folder wi... |
| `mcp__composio__OUTLOOK_COPY_MESSAGE_FROM_CHILD_FOLDER` | Tool to copy an email message from a child folder ... |
| `mcp__composio__OUTLOOK_COPY_MESSAGE_FROM_MAIL_FOLDER` | Tool to copy a message from a specific user's mail... |
| `mcp__composio__OUTLOOK_CREATE_ATTACHMENT_UPLOAD_SESSION` | Tool to create an upload session for large (>3 MB)... |
| `mcp__composio__OUTLOOK_CREATE_ATTACHMENT_UPLOAD_SESSION_IN_CHILD_FOLDER` | Tool to create an upload session for large (>3 MB)... |
| `mcp__composio__OUTLOOK_CREATE_CALENDAR` | Tool to create a new calendar in the signed-in use... |
| `mcp__composio__OUTLOOK_CREATE_CALENDAR_EVENT` | DEPRECATED: Use OUTLOOK_CREATE_USER_CALENDAR_CALEN... |
| `mcp__composio__OUTLOOK_CREATE_CALENDAR_EVENT_ATTACHMENT` | Tool to create a new attachment for an event in a ... |
| `mcp__composio__OUTLOOK_CREATE_CALENDAR_EVENT_ATTACHMENT_UPLOAD_SESSION` | Tool to create an upload session for large calenda... |

## When to Use This Integration

**Use Outlook when:**
- Interacting with Outlook services
- Automating Outlook workflows
- Reading and writing Outlook data

## Best Practices

1. **Prefer batch operations** when working with multiple items
2. **Check permissions** before performing write operations
3. **Handle errors gracefully** - MCP returns structured error responses
4. **Use appropriate tools** - choose the most specific tool for the task

## Authentication

- **Method**: OAuth via Composio
- **Status**: Connected and managed by Composio
- **Token Refresh**: Automatic

## Troubleshooting

If MCP tools are not available:
1. Check that `.mcp.json` is properly configured
2. Verify Composio API key is valid
3. Ensure the integration is connected in Composio dashboard
4. Restart Claude Code to reload MCP configuration

## Related Skills

Check Composio dashboard for related integrations.
