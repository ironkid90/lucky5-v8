---
name: wrike
description: Wrike is a project management and collaboration tool offering customizable workflows, Gantt charts, reporting, and resource management to boost team productivity integration via Composio MCP. Use when user needs to interact with Wrike.
---

# Wrike Integration

> Auto-generated by Composio Skills Extension on 2026-07-11T01:16:41.523Z
> **Execution**: Via Composio MCP Server (native Claude Code integration)

## Overview

Wrike is a project management and collaboration tool offering customizable workflows, Gantt charts, reporting, and resource management to boost team productivity

This skill uses **Composio MCP** for tool execution. Tools are available as native MCP tools
that Claude can call directly without any Bash commands or scripts.

## MCP Configuration

This integration requires Composio MCP server to be configured in `.mcp.json`:

```json
{
  "mcpServers": {
    "composio": {
      "command": "npx",
      "args": ["-y", "@composio/mcp"],
      "env": {
        "COMPOSIO_API_KEY": "your_api_key",
        "COMPOSIO_ENTITY_ID": "your_entity_id"
      }
    }
  }
}
```

## Available MCP Tools

### WRIKE_BULK_MODIFY_GROUPS

**MCP Tool**: `mcp__composio__WRIKE_BULK_MODIFY_GROUPS`

Adds or removes members for multiple Wrike groups in a single request; all specified user IDs must correspond to existing Wrike users.

**Parameters:**
  - `members` (array, required): List of group modification objects (max 20), each specifying a Wrike group and the members to add or remove.

### WRIKE_COPY_FOLDER

**MCP Tool**: `mcp__composio__WRIKE_COPY_FOLDER`

Copies a Wrike folder synchronously to a specified parent location with customizable options. Use when duplicating folder structures, optionally copying descriptions, responsibles, custom fields, statuses, and rescheduling tasks.

**Parameters:**
  - `title` (string, required): The title for the copied folder.
  - `parent` (string, required): The ID of the parent folder where the copy will be placed. Must be a valid Wrike folder ID.
  - `folderId` (string, required): The ID of the folder to copy. Must be a valid Wrike folder ID.
  - `entryLimit` (integer, optional): Maximum number of tasks/folders/projects to copy. Use this to limit the scope of the copy operation. If not specified, all entries will be copied.
  - `copyParents` (boolean, optional): If true, copies the parent folder hierarchy from the original folder. Defaults to false if not specified.
  - `copyStatuses` (boolean, optional): If true, copies statuses from the original folder to the new folder. Defaults to false if not specified.
  - `rescheduleMode` (string, optional): Reschedule mode for tasks in the copied folder. Valid values: 'Start' (reschedule based on start date) or 'End' (reschedule based on end date). Only used when rescheduledDate is provided.
  - `addResponsibles` (array, optional): List of user IDs to add as responsibles to the copied folder. These users will be assigned to the new folder regardless of the copyResponsibles setting.
  - `rescheduledDate` (string, optional): Date for rescheduling tasks in the copied folder. Must be in ISO 8601 format (e.g., '2024-12-31'). Used with rescheduleMode to adjust task dates.
  - `copyCustomFields` (boolean, optional): If true, copies custom fields from the original folder to the new folder. Defaults to false if not specified.
  - `copyDescriptions` (boolean, optional): If true, copies descriptions from the original folder to the new folder. Defaults to false if not specified.
  - `copyResponsibles` (boolean, optional): If true, copies assignees/responsibles from the original folder to the new folder. Defaults to false if not specified.
  - `copyCustomStatuses` (boolean, optional): If true, copies custom statuses from the original folder to the new folder. Defaults to false if not specified.
  - `removeResponsibles` (array, optional): List of user IDs to remove from responsibles in the copied folder. These users will not be assigned to the new folder even if present in the original.

### WRIKE_COPY_FOLDER_ASYNC

**MCP Tool**: `mcp__composio__WRIKE_COPY_FOLDER_ASYNC`

Duplicate a folder asynchronously in Wrike, creating a copy in a specified parent location. Use when copying large folder structures that may take time to complete. Returns an async job ID for tracking progress.

**Parameters:**
  - `title` (string, required): The name assigned to the copied folder. This is a required parameter for the async copy operation.
  - `parent` (string, required): The destination parent folder ID where the copy will be placed. Use WRIKE_GET_FOLDERS to get valid folder IDs.
  - `folder_id` (string, required): The identifier of the folder to be copied. Must be a valid Wrike folder ID.
  - `entry_limit` (integer, optional): Maximum number of tasks/folders/projects to copy. This limit must be between 1 and 250, inclusive. If the folder contains more items, only the specified number will be copied.
  - `copy_parents` (boolean, optional): Whether to maintain folder hierarchy when copying. If true, parent-child relationships are preserved in the copy.
  - `copy_statuses` (boolean, optional): Whether to preserve task statuses from the original folder. If true, tasks in the copy retain their status.
  - `reschedule_date` (string, optional): The date for task rescheduling in ISO 8601 format (YYYY-MM-DD). If provided along with reschedule_mode, tasks will be rescheduled based on this date.
  - `reschedule_mode` (string, optional): Mode for rescheduling tasks when copying a folder.
  - `add_responsibles` (array, optional): Additional users to assign to tasks in the new folder. Provide an array of Wrike user IDs.
  - `copy_descriptions` (boolean, optional): Whether to include the original folder's description in the copy. If true, descriptions are copied; otherwise, they are left empty.
  - `copy_responsibles` (boolean, optional): Whether to carry over original task assignments to the copied folder. If true, responsible users from original tasks are assigned to copied tasks.
  - `copy_custom_fields` (boolean, optional): Whether to replicate custom field data from the original folder to the copy. If true, custom field values are preserved.
  - `remove_responsibles` (array, optional): Users to exclude from task assignments in the copied folder. Provide an array of Wrike user IDs to remove.
  - `copy_custom_statuses` (boolean, optional): Whether to include custom status definitions in the copied folder. If true, custom statuses are replicated.

### WRIKE_CREATE_ACCOUNT_WEBHOOKS

**MCP Tool**: `mcp__composio__WRIKE_CREATE_ACCOUNT_WEBHOOKS`

Creates a webhook for the current account to receive notifications about changes. Use when you need to set up real-time event notifications for tasks, folders, or other Wrike objects.

**Parameters:**
  - `events` (array, optional): Array of event types to listen for (e.g., TaskCreated, TaskDeleted, FolderCreated). If omitted, all events are received.
  - `secret` (string, optional): Client secret for event authenticity verification. Used to validate that webhook events are genuinely from Wrike.
  - `hookUrl` (string, required): URL of the server that will receive the webhook payload. Must be a valid HTTPS URL.
  - `customItemTypes` (array, optional): Array of Custom Item Type IDs to limit webhooks to specific CIT changes. Only events related to these CITs will trigger the webhook.
  - `parameterisedEvents` (array, optional): Array of objects for filtering events with customizable fields. Each object specifies event type, custom fields, and standard fields to include.

### WRIKE_CREATE_ASSETS

**MCP Tool**: `mcp__composio__WRIKE_CREATE_ASSETS`

Tool to create equipment/asset in Wrike. Use when you need to add new equipment or assets to track in the system.

**Parameters:**
  - `name` (string, required): The name of the equipment/asset to create.

### WRIKE_CREATE_CUSTOMFIELD

**MCP Tool**: `mcp__composio__WRIKE_CREATE_CUSTOMFIELD`

Tool to create a new custom field in Wrike. Use when you need to define a new custom field for tasks, folders, or projects.

**Parameters:**
  - `type` (string, required): Type of the custom field. Determines what kind of data can be stored in this field.
  - `title` (string, required): Title of the custom field to create
  - `values` (array, optional): Array of predefined option values for DropDown and Multiple type custom fields.
  - `shareds` (array, optional): List of user IDs to share the custom field with. Empty list means not shared with anyone.
  - `spaceId` (string, optional): Space ID to create the custom field in. If not specified, creates an account-level custom field.
  - `contacts` (array, optional): Array of contact IDs representing allowed users or invitations for Contacts type custom fields.
  - `currency` (string, optional): Currency code for Currency type fields (e.g., USD, EUR, GBP). Defaults to USD if not specified.
  - `readOnly` (boolean, optional): Whether the custom field is read-only. Defaults to false.
  - `aggregation` (string, optional): Aggregation type for custom fields.
  - `decimalPlaces` (integer, optional): Number of decimal places for Numeric, Percentage, and Currency types. Defaults to 2 if not specified.
  - `inheritanceType` (string, optional): Inheritance type for custom fields.
  - `allowOtherValues` (boolean, optional): Whether users can enter custom values beyond predefined options for DropDown type. Defaults to true.
  - `useThousandsSeparator` (boolean, optional): Whether to use thousands separator for Numeric type fields. Defaults to false.

### WRIKE_CREATE_FOLDER

**MCP Tool**: `mcp__composio__WRIKE_CREATE_FOLDER`

Creates a new Wrike subfolder within the specified `folderId`, optionally as a project if `customItemTypeId` is given; the folder is auto-shared with its creator.

**Parameters:**
  - `title` (string, required): The title for the new folder.
  - `shareds` (array, optional): A list of user IDs or invited user emails to share the folder with.
  - `folderId` (string, required): The unique identifier of the parent folder where the new subfolder will be created.
  - `metadata` (array, optional): A list of metadata entries (key-value pairs) to associate with the newly created folder.
  - `description` (string, optional): An optional description for the new folder.
  - `customFields` (array, optional): A list of custom field entries (ID and value) to set for the new folder.
  - `customColumns` (array, optional): A list of custom field IDs to be associated with the folder as columns. These typically control the visibility and order of custom fields in a table view.
  - `userAccessRoles` (object, optional): A dictionary mapping user IDs to their access roles for this folder (e.g., 'Editor', 'Limited', 'Full', 'Owner'). The key is the user ID and the value is the access role name.
  - `withInvitations` (boolean, optional): A boolean flag indicating whether to include invited (pending) users in the 'ownerIds' and 'sharedIds' lists in the response.
  - `customItemTypeId` (string, optional): The ID of a Custom Item Type. If provided, the created folder will be a project based on this type.
  - `plainTextCustomFields` (boolean, optional): A boolean flag. If true, HTML tags will be stripped from custom field values.

### WRIKE_CREATE_FOLDER_BOOKING

**MCP Tool**: `mcp__composio__WRIKE_CREATE_FOLDER_BOOKING`

Tool to create a booking in a Wrike folder. Use when you need to allocate resources (users or placeholders) to a folder for a specific date range. Either responsibleId or responsiblePlaceholderId must be provided, and bookingDates must include at least two of: startDate, finishDate, or duration.

**Parameters:**
  - `folderId` (string, required): Unique identifier of the folder to create the booking in
  - `bookingDates` (object, required): Object containing booking date information. Must include two of: startDate, finishDate, or duration.
  - `responsibleId` (string, optional): Contact ID of the user responsible for this booking. Either responsibleId or responsiblePlaceholderId must be provided.
  - `effortAllocation` (object, optional): Effort allocation details for the booking.
  - `responsiblePlaceholderId` (string, optional): Placeholder ID if booking is for a placeholder resource instead of a user. Either responsibleId or responsiblePlaceholderId must be provided.

### WRIKE_CREATE_FOLDERS_APPROVALS

**MCP Tool**: `mcp__composio__WRIKE_CREATE_FOLDERS_APPROVALS`

Tool to create a new approval in a Wrike folder. Use when you need to create an approval workflow for folder content review.

**Parameters:**
  - `folderId` (string, required): The unique identifier of the folder where the approval will be created

### WRIKE_CREATE_FOLDERS_COMMENTS

**MCP Tool**: `mcp__composio__WRIKE_CREATE_FOLDERS_COMMENTS`

Creates a new comment on a specified Wrike folder. Use when adding feedback, updates, or notes to a folder.

**Parameters:**
  - `text` (string, required): The comment text content. HTML format is accepted with limited tags.
  - `folderId` (string, required): The unique identifier of the folder to add the comment to.

### WRIKE_CREATE_FOLDERS_TIMELOG_LOCK_PERIOD

**MCP Tool**: `mcp__composio__WRIKE_CREATE_FOLDERS_TIMELOG_LOCK_PERIOD`

Creates a timelog lock period for a specific folder. Timelogs on or before the specified period date will be locked and cannot be edited. Use when you need to prevent modifications to timelog entries up to a certain date.

**Parameters:**
  - `period` (string, required): Date in YYYY-MM-DD format specifying the timelog lock period to create. Timelogs on or before this date will be locked.
  - `folderId` (string, required): Unique identifier of the folder for which to create a timelog lock period.

### WRIKE_CREATE_FOLDER_WEBHOOK

**MCP Tool**: `mcp__composio__WRIKE_CREATE_FOLDER_WEBHOOK`

Tool to create a webhook for a Wrike folder or project. Use when you need to set up real-time notifications for events occurring within a specific folder or its subfolders.

**Parameters:**
  - `events` (array, optional): Optional list of events to subscribe to. If not specified, all events will be received. Common events include: 'AttachmentAdded', 'AttachmentDeleted', 'TaskCreated', 'TaskDeleted', 'TaskStatusChanged', 'CommentAdded', 'CommentDeleted', etc.
  - `secret` (string, optional): Optional client secret to verify the authenticity of webhook events. This secret will be used to sign webhook payloads.
  - `hookUrl` (string, required): The URL of the server that will receive the webhook payload when events occur. Must be a valid HTTPS URL.
  - `folder_id` (string, required): The unique identifier of the folder or project to create the webhook for. Must be a valid Wrike folder ID (e.g., 'IEAGTXR7I4IHGABC').
  - `recursive` (boolean, optional): Whether to listen to events for subfolders and subprojects in the hierarchy. Default is false. Set to true to track all nested elements.

### WRIKE_CREATE_GROUP

**MCP Tool**: `mcp__composio__WRIKE_CREATE_GROUP`

Creates a new user group in Wrike with a specified title, optionally setting members, parent group, avatar, and custom metadata.

**Parameters:**
  - `title` (string, required): The title or name of the new group.
  - `parent` (string, optional): The contact ID of an existing group to set as the parent for this new group. If omitted, the group will be a top-level group.
  - `members` (array, optional): List of Wrike user contact IDs to add as group members.
  - `metadata` (array, optional): List of key-value metadata entries to associate with the group.
  - `avatar__color` (string, optional): The hex color code for the group's avatar background (e.g., '#FF0000' for red).
  - `avatar__letters` (string, optional): Up to two characters to display as the group's avatar. For example, 'MT' for 'Marketing Team'.

### WRIKE_CREATE_INVITATION

**MCP Tool**: `mcp__composio__WRIKE_CREATE_INVITATION`

Invites a user to a Wrike workspace by email, optionally with name, specifying EITHER `userTypeId` OR a combination of `role`/`external`; custom email subject/message available for paid accounts.

**Parameters:**
  - `role` (string, optional): Valid values for the Wrike invitation role parameter.
  - `email` (string, required): Email address of the user to invite.
  - `message` (string, optional): Custom message body for the invitation email (paid Wrike accounts only).
  - `subject` (string, optional): Custom subject for the invitation email (paid Wrike accounts only).
  - `external` (boolean, optional): Flags invited user as external. Cannot be used if `userTypeId` is specified. Can be combined with `role`.
  - `lastName` (string, optional): Last name of the invited user.
  - `firstName` (string, optional): First name of the invited user.
  - `userTypeId` (string, optional): ID of the user type to assign (e.g., Admin, Regular User, External User). Cannot be used if `role` or `external` is specified.

### WRIKE_CREATE_SPACES_TIMELOG_LOCK_PERIOD

**MCP Tool**: `mcp__composio__WRIKE_CREATE_SPACES_TIMELOG_LOCK_PERIOD`

Tool to create a timelog lock period for a space to prevent time entries from being edited. Use when you need to lock timelogs for specific dates, typically before creating payrolls or preparing invoices.

**Parameters:**
  - `period` (string, required): Date for the timelog lock period in YYYY-MM-DD format. This date and all prior dates will be locked for timelog editing.
  - `spaceId` (string, required): Unique identifier of the space to create a timelog lock period for.

### WRIKE_CREATE_SPACE_WEBHOOK

**MCP Tool**: `mcp__composio__WRIKE_CREATE_SPACE_WEBHOOK`

Tool to create a webhook on a Wrike space for receiving notifications about changes to tasks, folders, and projects. Use when you need to set up event-driven integrations for space-level changes.

**Parameters:**
  - `events` (array, optional): List of specific event types to subscribe to. If omitted, all events are received. Examples: AttachmentAdded, AttachmentDeleted, TaskCreated, TaskTitleChanged, etc.
  - `secret` (string, optional): Client secret for verifying event authenticity and securing webhook payloads.
  - `hook_url` (string, required): URL of the server endpoint that will receive the webhook payload notifications.
  - `space_id` (string, required): Unique identifier of the space to create the webhook for.
  - `recursive` (boolean, optional): If true, monitor subfolders and subprojects throughout the space hierarchy. Default is false.

### WRIKE_CREATE_TASK

**MCP Tool**: `mcp__composio__WRIKE_CREATE_TASK`

Creates a new task in a specified Wrike folder; if setting priority with `priorityBefore` or `priorityAfter`, the referenced task must be in the same folder or project.

**Parameters:**
  - `title` (string, required): Title of the task.
  - `follow` (boolean, optional): If `True`, the creator automatically follows the task.
  - `status` (string, optional): Status of the task. Note: This feature may not be available on the Team plan.
  - `parents` (array, optional): List of parent folder IDs for the task. Cannot include the recycleBinId.
  - `shareds` (array, optional): List of user or invitation IDs to share the task with; creator is auto-shared.
  - `folderId` (string, required): Unique identifier of the folder where the task will be created.
  - `metadata` (array, optional): List of key-value metadata pairs for the task. Each item requires 'key' and 'value'.
  - `followers` (array, optional): List of user IDs to be added as task followers.
  - `importance` (string, optional): Importance level of the task.
  - `superTasks` (array, optional): List of task IDs to which this new task will be a subtask.
  - `billingType` (string, optional): Billing type for the task's timelogs.
  - `description` (string, optional): Detailed description of the task.
  - `customFields` (array, optional): List of custom fields for the task. Each item requires `id` (custom field ID) and `value`. Refer to Wrike API documentation for value formatting based on type.
  - `customStatus` (string, optional): Unique ID of a custom status for the task.
  - `responsibles` (array, optional): List of user or invitation IDs to be assigned as responsible for the task.
  - `priorityAfter` (string, optional): ID of an existing task to place the new task after in the list.
  - `priorityBefore` (string, optional): ID of an existing task to place the new task before in the list.
  - `withInvitations` (boolean, optional): If `True`, include invited (unregistered) users in response's `shareds` and `responsibles`.
  - `customItemTypeId` (string, optional): Unique ID of a Custom Item Type to use as a template for the task; task inherits its properties.
  - `plainTextCustomFields` (boolean, optional): If `True`, HTML tags will be stripped from custom field values before saving.
  - `responsiblePlaceholders` (array, optional): List of placeholder IDs (Job Roles) to be assigned as responsible for the task.

### WRIKE_CREATE_TASK_ATTACHMENT

**MCP Tool**: `mcp__composio__WRIKE_CREATE_TASK_ATTACHMENT`

Tool to upload a file attachment to a Wrike task. Use when you need to attach files to tasks.

**Parameters:**
  - `file` (string, required): File to upload to the task.
  - `taskId` (string, required): The ID of the task to attach the file to.

### WRIKE_CREATE_TASK_COMMENT

**MCP Tool**: `mcp__composio__WRIKE_CREATE_TASK_COMMENT`

Tool to create a new comment on a Wrike task. Use when you need to add a comment or note to a task.

**Parameters:**
  - `text` (string, required): Comment text content. Supports HTML formatting with limited tags. Use HTML syntax for @mentions.
  - `taskId` (string, required): The ID of the task to add a comment to.

### WRIKE_CREATE_TASKS_APPROVALS

**MCP Tool**: `mcp__composio__WRIKE_CREATE_TASKS_APPROVALS`

Tool to create an approval on a specific task in Wrike. Use when you need to add an approval workflow to a task.

**Parameters:**
  - `taskId` (string, required): Unique identifier of the task to add the approval to


## Usage Examples

#### WRIKE_BULK_MODIFY_GROUPS
```
Use MCP tool: mcp__composio__WRIKE_BULK_MODIFY_GROUPS
Parameters: {
  "members": "value"
}
```

#### WRIKE_COPY_FOLDER
```
Use MCP tool: mcp__composio__WRIKE_COPY_FOLDER
Parameters: {
  "folderId": "your_folderId_here",
  "title": "your_title_here"
}
```

#### WRIKE_COPY_FOLDER_ASYNC
```
Use MCP tool: mcp__composio__WRIKE_COPY_FOLDER_ASYNC
Parameters: {
  "folder_id": "your_folder_id_here",
  "parent": "your_parent_here"
}
```

## Quick Reference

| MCP Tool | Description |
|----------|-------------|
| `mcp__composio__WRIKE_BULK_MODIFY_GROUPS` | Adds or removes members for multiple Wrike groups ... |
| `mcp__composio__WRIKE_COPY_FOLDER` | Copies a Wrike folder synchronously to a specified... |
| `mcp__composio__WRIKE_COPY_FOLDER_ASYNC` | Duplicate a folder asynchronously in Wrike, creati... |
| `mcp__composio__WRIKE_CREATE_ACCOUNT_WEBHOOKS` | Creates a webhook for the current account to recei... |
| `mcp__composio__WRIKE_CREATE_ASSETS` | Tool to create equipment/asset in Wrike. Use when ... |
| `mcp__composio__WRIKE_CREATE_CUSTOMFIELD` | Tool to create a new custom field in Wrike. Use wh... |
| `mcp__composio__WRIKE_CREATE_FOLDER` | Creates a new Wrike subfolder within the specified... |
| `mcp__composio__WRIKE_CREATE_FOLDER_BOOKING` | Tool to create a booking in a Wrike folder. Use wh... |
| `mcp__composio__WRIKE_CREATE_FOLDERS_APPROVALS` | Tool to create a new approval in a Wrike folder. U... |
| `mcp__composio__WRIKE_CREATE_FOLDERS_COMMENTS` | Creates a new comment on a specified Wrike folder.... |
| `mcp__composio__WRIKE_CREATE_FOLDERS_TIMELOG_LOCK_PERIOD` | Creates a timelog lock period for a specific folde... |
| `mcp__composio__WRIKE_CREATE_FOLDER_WEBHOOK` | Tool to create a webhook for a Wrike folder or pro... |
| `mcp__composio__WRIKE_CREATE_GROUP` | Creates a new user group in Wrike with a specified... |
| `mcp__composio__WRIKE_CREATE_INVITATION` | Invites a user to a Wrike workspace by email, opti... |
| `mcp__composio__WRIKE_CREATE_SPACES_TIMELOG_LOCK_PERIOD` | Tool to create a timelog lock period for a space t... |
| `mcp__composio__WRIKE_CREATE_SPACE_WEBHOOK` | Tool to create a webhook on a Wrike space for rece... |
| `mcp__composio__WRIKE_CREATE_TASK` | Creates a new task in a specified Wrike folder; if... |
| `mcp__composio__WRIKE_CREATE_TASK_ATTACHMENT` | Tool to upload a file attachment to a Wrike task. ... |
| `mcp__composio__WRIKE_CREATE_TASK_COMMENT` | Tool to create a new comment on a Wrike task. Use ... |
| `mcp__composio__WRIKE_CREATE_TASKS_APPROVALS` | Tool to create an approval on a specific task in W... |

## When to Use This Integration

**Use Wrike when:**
- Interacting with Wrike services
- Automating Wrike workflows
- Reading and writing Wrike data

## Best Practices

1. **Prefer batch operations** when working with multiple items
2. **Check permissions** before performing write operations
3. **Handle errors gracefully** - MCP returns structured error responses
4. **Use appropriate tools** - choose the most specific tool for the task

## Authentication

- **Method**: OAuth via Composio
- **Status**: Connected and managed by Composio
- **Token Refresh**: Automatic

## Troubleshooting

If MCP tools are not available:
1. Check that `.mcp.json` is properly configured
2. Verify Composio API key is valid
3. Ensure the integration is connected in Composio dashboard
4. Restart Claude Code to reload MCP configuration

## Related Skills

Check Composio dashboard for related integrations.
